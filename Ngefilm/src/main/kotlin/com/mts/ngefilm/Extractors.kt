package com.mts.ngefilm

import com.lagradost.cloudstream3.SubtitleFile
import com.lagradost.cloudstream3.app
import com.lagradost.cloudstream3.extractors.StreamWishExtractor
import com.lagradost.cloudstream3.utils.ExtractorApi
import com.lagradost.cloudstream3.utils.ExtractorLink
import com.lagradost.cloudstream3.utils.ExtractorLinkType
import com.lagradost.cloudstream3.utils.Qualities
import com.lagradost.cloudstream3.utils.loadExtractor
import com.lagradost.cloudstream3.utils.newExtractorLink
import javax.crypto.Cipher
import javax.crypto.spec.IvParameterSpec
import javax.crypto.spec.SecretKeySpec

class Ratu89News : StreamWishExtractor() {
    override var name = "Ratu89News"
    override var mainUrl = "https://ratu89.news"
}

class ApkblockS3Apnortheast1AmazonawsCom : StreamWishExtractor() {
    override var name = "ApkblockS3Apnortheast1AmazonawsCom"
    override var mainUrl = "https://apk-block.s3.ap-northeast-1.amazonaws.com"
}

class Gratu89Com : StreamWishExtractor() {
    override var name = "Gratu89Com"
    override var mainUrl = "https://gratu89.com"
}

class Judi89News : StreamWishExtractor() {
    override var name = "Judi89News"
    override var mainUrl = "https://judi89.news"
}

class FkuponCom : StreamWishExtractor() {
    override var name = "FkuponCom"
    override var mainUrl = "https://fkupon.com"
}

class BioSite : StreamWishExtractor() {
    override var name = "BioSite"
    override var mainUrl = "https://bio.site"
}

class HalalhomecookingCom : StreamWishExtractor() {
    override var name = "HalalhomecookingCom"
    override var mainUrl = "https://halalhomecooking.com"
}

class WapBm88Net : StreamWishExtractor() {
    override var name = "WapBm88Net"
    override var mainUrl = "https://wap.bm-88.net"
}

class HistoryJlfafafa3Com : StreamWishExtractor() {
    override var name = "HistoryJlfafafa3Com"
    override var mainUrl = "https://history.jlfafafa3.com"
}

class CdnAmpprojectOrg : StreamWishExtractor() {
    override var name = "CdnAmpprojectOrg"
    override var mainUrl = "https://cdn.ampproject.org"
}

class CampaignPastiwddarikdslotCom : StreamWishExtractor() {
    override var name = "CampaignPastiwddarikdslotCom"
    override var mainUrl = "https://campaign.pastiwddarikdslot.com"
}

class KdslotmainCom : StreamWishExtractor() {
    override var name = "KdslotmainCom"
    override var mainUrl = "https://kdslotmain.com"
}

class KdslotidnCom : StreamWishExtractor() {
    override var name = "KdslotidnCom"
    override var mainUrl = "https://kdslotidn.com"
}

class KdpokersCom : StreamWishExtractor() {
    override var name = "KdpokersCom"
    override var mainUrl = "https://kdpokers.com"
}

class ApkbersamakdCom : StreamWishExtractor() {
    override var name = "ApkbersamakdCom"
    override var mainUrl = "https://apkbersamakd.com"
}

class TautinApp : StreamWishExtractor() {
    override var name = "TautinApp"
    override var mainUrl = "https://tautin.app"
}

class CdnLivechatincCom : StreamWishExtractor() {
    override var name = "CdnLivechatincCom"
    override var mainUrl = "https://cdn.livechatinc.com"
}

class Jseu1HsscriptsCom : StreamWishExtractor() {
    override var name = "Jseu1HsscriptsCom"
    override var mainUrl = "https://js-eu1.hs-scripts.com"
}

class MyLivechatincCom : StreamWishExtractor() {
    override var name = "MyLivechatincCom"
    override var mainUrl = "https://my.livechatinc.com"
}

class AccountsLivechatCom : StreamWishExtractor() {
    override var name = "AccountsLivechatCom"
    override var mainUrl = "https://accounts.livechat.com"
}

class TextCom : StreamWishExtractor() {
    override var name = "TextCom"
    override var mainUrl = "https://text.com"
}

class PlatformTextCom : StreamWishExtractor() {
    override var name = "PlatformTextCom"
    override var mainUrl = "https://platform.text.com"
}

class LineMe : StreamWishExtractor() {
    override var name = "LineMe"
    override var mainUrl = "https://line.me"
}

class CMicrosoftCom : StreamWishExtractor() {
    override var name = "CMicrosoftCom"
    override var mainUrl = "https://c.microsoft.com"
}

class StaticsTeamsCdnLiveNet : StreamWishExtractor() {
    override var name = "StaticsTeamsCdnLiveNet"
    override var mainUrl = "https://statics.teams.cdn.live.net"
}

class ApiLivechatincCom : StreamWishExtractor() {
    override var name = "ApiLivechatincCom"
    override var mainUrl = "https://api.livechatinc.com"
}

class KdslotproSpace : StreamWishExtractor() {
    override var name = "KdslotproSpace"
    override var mainUrl = "https://kdslotpro.space"
}

class DewajudiInk : StreamWishExtractor() {
    override var name = "DewajudiInk"
    override var mainUrl = "https://dewajudi.ink"
}

class ApkdepotS3Apnortheast1AmazonawsCom : StreamWishExtractor() {
    override var name = "ApkdepotS3Apnortheast1AmazonawsCom"
    override var mainUrl = "https://apk-depot.s3.ap-northeast-1.amazonaws.com"
}

class HgcloudTo : StreamWishExtractor() {
    override var name = "HgcloudTo"
    override var mainUrl = "https://hgcloud.to"
}

class MorenciusCom : StreamWishExtractor() {
    override var name = "MorenciusCom"
    override var mainUrl = "https://morencius.com"
}

class IYtimgCom : StreamWishExtractor() {
    override var name = "IYtimgCom"
    override var mainUrl = "https://i.ytimg.com"
}

class YoutuBe : StreamWishExtractor() {
    override var name = "YoutuBe"
    override var mainUrl = "https://youtu.be"
}

class KdslotaksesKdslotidnShop : StreamWishExtractor() {
    override var name = "KdslotaksesKdslotidnShop"
    override var mainUrl = "https://kdslotakses.kdslotidn.shop"
}

class KdslolacsOnline : StreamWishExtractor() {
    override var name = "KdslolacsOnline"
    override var mainUrl = "https://kdslolacs.online"
}

class LivechatCom : StreamWishExtractor() {
    override var name = "LivechatCom"
    override var mainUrl = "https://livechat.com"
}

class TeamsLiveCom : StreamWishExtractor() {
    override var name = "TeamsLiveCom"
    override var mainUrl = "https://teams.live.com"
}

class PlayerXExtractor : ExtractorApi() {
    override var name = "PlayerXExtractor"
    override var mainUrl = "https://ezplayer.stream"
    override val requiresReferer = true

    private fun decryptAesCbc(ciphertext: ByteArray, key: ByteArray, iv: ByteArray): ByteArray {
        val spec = javax.crypto.spec.SecretKeySpec(key, "AES")
        val parameterSpec = javax.crypto.spec.IvParameterSpec(iv)
        val cipher = javax.crypto.Cipher.getInstance("AES/CBC/PKCS7Padding")
        cipher.init(javax.crypto.Cipher.DECRYPT_MODE, spec, parameterSpec)
        return cipher.doFinal(ciphertext)
    }

    override suspend fun getUrl(
        url: String,
        referer: String?,
        subtitleCallback: (SubtitleFile) -> Unit,
        callback: (ExtractorLink) -> Unit
    ) {
        try {
            val cleanUrl = url.replace(92.toChar().toString(), "")
            val uri = java.net.URI(cleanUrl)
            val host = uri.host ?: "playerx.ezplayer.stream"
            val hash = uri.fragment ?: ""
            if (hash.isEmpty()) return
            val id = hash.replace("#", "")
            if (id.isEmpty()) return

            val parentReferer = referer ?: "https://ww105.pencurimoviesubmalay.guru/"
            val videoUrl = "https://$host/api/v1/video?id=$id"

            val responseHex = app.get(videoUrl, headers = mapOf("Referer" to parentReferer)).text.trim()
            if (responseHex.isEmpty()) return

            val ciphertext = responseHex.chunked(2).map { it.toInt(16).toByte() }.toByteArray()
            val key = "kiemtienmua911ca".toByteArray(Charsets.UTF_8)
            val iv = "1234567890oiuytr".toByteArray(Charsets.UTF_8)

            val decryptedBytes = decryptAesCbc(ciphertext, key, iv)
            val decryptedStr = String(decryptedBytes, Charsets.UTF_8)

            val metadata = org.json.JSONObject(decryptedStr)
            val pk = metadata.optJSONObject("pk")
            val k = pk?.optString("k") ?: ""
            val kx = pk?.optString("kx") ?: ""
            val title = metadata.optString("title", "PlayerX Stream")

            val qualityVal = when {
                title.contains("2160p", true) || title.contains("4k", true) -> Qualities.P2160.value
                title.contains("1080p", true) -> Qualities.P1080.value
                title.contains("720p", true) -> Qualities.P720.value
                title.contains("480p", true) -> Qualities.P480.value
                title.contains("360p", true) -> Qualities.P360.value
                else -> Qualities.Unknown.value
            }

            val cfNative = metadata.optString("cfNative")
            val source = metadata.optString("source")

            if (cfNative.isNotEmpty()) {
                var finalUrl = cfNative
                if (k.isNotEmpty() && !finalUrl.contains("k=")) {
                    finalUrl += if (finalUrl.contains("?")) "&k=$k&kx=$kx" else "?k=$k&kx=$kx"
                }
                callback(
                    newExtractorLink(
                        source = "PlayerX",
                        name = "PlayerX (CF)",
                        url = finalUrl,
                        type = ExtractorLinkType.M3U8
                    ) {
                        this.referer = "https://$host/"
                        this.quality = qualityVal
                    }
                )
            }

            if (source.isNotEmpty()) {
                var finalUrl = source
                if (k.isNotEmpty() && !finalUrl.contains("k=")) {
                    finalUrl += if (finalUrl.contains("?")) "&k=$k&kx=$kx" else "?k=$k&kx=$kx"
                }
                callback(
                    newExtractorLink(
                        source = "PlayerX",
                        name = "PlayerX Direct",
                        url = finalUrl,
                        type = ExtractorLinkType.M3U8
                    ) {
                        this.referer = "https://$host/"
                        this.quality = qualityVal
                    }
                )
            }
        } catch (e: Exception) {
            e.printStackTrace()
        }
    }
}
